import { extendTheme } from 'native-base';

const theme = extendTheme({
  colors: {
    background: '#FEFEFE',
  },

  fonts: {
    bold: 'Bold',
    medium: 'Medium',
    regular: 'Regular',
  },
});

export { theme };
